bin/
# Release archives and the portal build, both reproducible from a tag.
dist/
node_modules/
shots/
packages/*/harness/*.js

# Local env overrides
.env
.env.local

.DS_Store

# A development deployment: accounts, and the database they live in.
.dev/
