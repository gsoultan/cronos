import { PlaceholderPage } from './PlaceholderPage'

export function DashboardsPage() {
  return (
    <PlaceholderPage
      title="Dashboards"
      description="Live views your team can keep open all day."
      emptyTitle="No dashboards yet"
      emptyDescription="A dashboard combines tiles from several reports behind one shared set of filters."
      cta="New dashboard"
    />
  )
}
