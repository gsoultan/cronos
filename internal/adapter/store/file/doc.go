// Package file serves definitions from a directory of YAML.
//
// The development and single-tenant story: point cronos at a git repository of
// definitions and run it. A Postgres-backed repository with content-addressed
// versions is what a hosted deployment needs, and it implements the same ports.
package file
