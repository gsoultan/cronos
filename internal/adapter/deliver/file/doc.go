// Package file writes rendered documents to a directory.
//
// The channel every deployment can use without configuring anything, and the
// one a burst can be tested against: an assertion about a file on disk is an
// assertion about what a recipient received. Email and object storage
// implement the same port.
package file
